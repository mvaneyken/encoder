# encode
ROR app that maintains scripts that can be used to classify something. The classification can be run on the encode server or downloaded & run on an iOS app (divine).

# Development environment
* Ruby
* Rails
* Postgresql

WARNING: This is simply a proof of concept. It's not yet ready for prime time.
